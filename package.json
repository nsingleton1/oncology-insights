{
  "name": "oncology-insights",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "@headlessui/react": "^2.0.0",
    "@heroicons/react": "^2.0.18",
    "@testing-library/jest-dom": "^5.17.0",
    "@testing-library/react": "^13.4.0",
    "@testing-library/user-event": "^13.5.0",
    "http-proxy-middleware": "^2.0.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-hot-toast": "^2.5.2",
    "react-markdown": "^10.1.0",
    "react-scripts": "5.0.1",
    "recharts": "^2.15.3",
    "serve": "^14.2.1",
    "tailwindcss": "^3.4.1",
    "uuid": "^11.1.0",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "prebuild": "node scripts/create-placeholder-images.js || echo 'Placeholder script skipped' && node scripts/verify-dependencies.js || echo 'Verify script skipped'",
    "build": "react-scripts build",
    "postbuild": "node scripts/prepare-deploy.js || echo 'Prepare deploy script skipped' && node scripts/post-deployment.js || echo 'Post deployment script skipped'",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "serve": "serve -s build",
    "verify": "node scripts/verify-dependencies.js || echo 'Verify script skipped'",
    "edit:add": "git add .",
    "git::commit": "git commit -m",
    "git::setup-remote": "git remote add origin",
    "git:push": "git push origin"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1"
  }
} 